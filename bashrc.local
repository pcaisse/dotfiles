GIT_PROMPT_ONLY_IN_REPO=1
source ~/.bash-git-prompt/gitprompt.sh

alias vim='nvim'
alias v='nvim'
export EDITOR=nvim

alias clipboard='xclip -se c'

source ~/.xprofile

# turn off middle button pasting (only scroll)
xmodmap -e "pointer = 1 25 3 4 5 6 7 8 9"
